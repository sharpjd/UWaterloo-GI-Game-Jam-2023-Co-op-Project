using UnityEngine;

public class MapPathPoint : MonoBehaviour
{

    public int NumberInPointSequence;

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
